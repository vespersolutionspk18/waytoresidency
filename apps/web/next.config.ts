import type { NextConfig } from 'next';

// API + auth now ship as native Next.js Route Handlers under /app/api/*.
// No rewrites needed — everything is same-origin.
const nextConfig: NextConfig = {};

export default nextConfig;
